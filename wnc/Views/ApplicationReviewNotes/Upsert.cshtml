@model ApplicationReviewNoteFormModel
@{
    var actionName = ViewContext.RouteData.Values["action"]?.ToString() ?? "Create";
    var isEdit = string.Equals(actionName, "Edit", StringComparison.OrdinalIgnoreCase);
    ViewData["Title"] = isEdit ? "Edit Application Review Note" : "Create Application Review Note";
    var routeId = ViewContext.RouteData.Values["id"];
}

<h1 class="h3 mb-4">@ViewData["Title"]</h1>

<form asp-action="@actionName" asp-route-id="@routeId" method="post" class="row g-3">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="col-md-6">
        <label asp-for="ApplicationId" class="form-label"></label>
        <select asp-for="ApplicationId" asp-items="Model.ApplicationOptions" class="form-select"></select>
        <span asp-validation-for="ApplicationId" class="text-danger"></span>
    </div>
    <div class="col-md-6">
        <label asp-for="AuthorUserId" class="form-label"></label>
        <select asp-for="AuthorUserId" asp-items="Model.UserOptions" class="form-select"></select>
        <span asp-validation-for="AuthorUserId" class="text-danger"></span>
    </div>
    <div class="col-md-6">
        <label asp-for="NoteType" class="form-label"></label>
        <input asp-for="NoteType" class="form-control" />
        <span asp-validation-for="NoteType" class="text-danger"></span>
    </div>
    <div class="col-md-6">
        <label asp-for="CreatedAt" class="form-label"></label>
        <input asp-for="CreatedAt" class="form-control" type="datetime-local" asp-format="{0:yyyy-MM-ddTHH:mm}" />
        <span asp-validation-for="CreatedAt" class="text-danger"></span>
    </div>
    <div class="col-12">
        <div class="form-check">
            <input asp-for="IsVisibleToCandidate" class="form-check-input" />
            <label asp-for="IsVisibleToCandidate" class="form-check-label"></label>
        </div>
    </div>
    <div class="col-12">
        <label asp-for="Content" class="form-label"></label>
        <textarea asp-for="Content" class="form-control" rows="8"></textarea>
        <span asp-validation-for="Content" class="text-danger"></span>
    </div>
    <div class="col-12 d-flex gap-2">
        <button type="submit" class="btn btn-primary">Save</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
