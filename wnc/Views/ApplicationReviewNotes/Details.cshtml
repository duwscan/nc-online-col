@model ApplicationReviewNote
@{
    ViewData["Title"] = "Application Review Note Details";
}

<h1 class="h3 mb-4">Application Review Note Details</h1>

<dl class="row">
    <dt class="col-sm-3">Application</dt>
    <dd class="col-sm-9">@Model.Application.ApplicationCode</dd>
    <dt class="col-sm-3">Author</dt>
    <dd class="col-sm-9">@(Model.AuthorUser.Username ?? Model.AuthorUser.Email)</dd>
    <dt class="col-sm-3">Note type</dt>
    <dd class="col-sm-9">@Model.NoteType</dd>
    <dt class="col-sm-3">Visible to candidate</dt>
    <dd class="col-sm-9">@(Model.IsVisibleToCandidate ? "Yes" : "No")</dd>
    <dt class="col-sm-3">Created at</dt>
    <dd class="col-sm-9">@Model.CreatedAt.ToString("yyyy-MM-dd HH:mm")</dd>
    <dt class="col-sm-3">Content</dt>
    <dd class="col-sm-9" style="white-space: pre-wrap">@Model.Content</dd>
</dl>

<div class="d-flex gap-2">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
    <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
    <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
        <button type="submit" class="btn btn-outline-danger" onclick="return confirm('Delete this record?');">Delete</button>
    </form>
</div>
